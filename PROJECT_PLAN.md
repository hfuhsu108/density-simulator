# Density_experiment — 專案計畫與裁示紀錄

**模擬主題**：液體的體積與質量關係（國中理化「密度」單元）
**核心教學命題**：M = D × V + M<sub>量筒</sub>。**斜率＝液體密度、截距＝量筒質量**；
同液體換量筒 → 兩線平行；換密度 → 斜率改變。
**檔案**：`index.html`（單檔交付）。`*.dc.html` 與 `support.js` 是 2026-07-27 的 Claude Design 稿，非執行檔。

---

## 2026-08-22　依運動學模擬器經驗做規範對齊

`_shared/` 的 `DEV_SPEC.md` / `DESIGN_SPEC.md` / `template/index.html` 在 2026-08-20～22 依
motion-graphs-sim 的實作經驗大幅增修（新增 5.4 等比放大、第 7 節圖表行為、第 2 節對比度門檻、
第 9 節教學內容規範等）。本模擬完成於 2026-08-18，停在舊版規範，本次補齊。

### 使用者裁示（4 題，AskUserQuestion）

| # | 問題 | 裁示 | 取代了什麼 |
|---|---|---|---|
| 1 | 座標軸範圍 | **全部固定**，超出裁切＋出界標記 | 取代原本「依資料自動縮放」（`Math.ceil(maxV…)`／`nice()`）。模擬與實驗兩模式共用同一組固定軸 `V 0–60 / M 0–160` |
| 2 | 自訂密度滑桿 | **五檔 0.6 / 0.8 / 1.0 / 1.2 / 1.4** | 取代原本 `step="0.01"`（81 檔連續）。預設值一併由 1.05 改為 1.00（原值不在檔位上） |
| 3 | 輔助疊層預設狀態 | **「顯示直線與截距」「顯示格線」都維持預設開** | 明確**不採用** `DESIGN_SPEC` 第 4 節「教學輔助疊層一律預設關」。理由：這個模擬的核心命題就是那條直線，預設關會讓學生第一眼只看到散點 |
| 4 | 擬合直線範圍 | **維持畫滿全寬**（外插到尚未量測的體積） | 明確**不採用** `DESIGN_SPEC` 第 7 節「不劇透」。理由：「照這條線，40 mL 應該是幾克？」的預測活動是本模擬的教學重點 |

### 規範偏離登記（`DEV_SPEC` 10.4 要求）

以下每一條都與 `_shared/` 的現行規範不同，**是刻意的，不要改回去**：

1. **輔助疊層預設開**（裁示 3）— 偏離 `DESIGN_SPEC` 第 4 節。
2. **擬合線外插畫滿全寬**（裁示 4）— 偏離 `DESIGN_SPEC` 第 7 節「不劇透」。
3. **密度滑桿檔位是小數**（0.6/0.8/…）— `DESIGN_SPEC` 第 4 節寫「禁止小數檔位」，
   但那條是為運動學的加速度寫的；密度本質就是小數，改成整數無意義。檔數（5）仍遵守 3–7 檔。
4. **座標軸線改用 `#7A8496`** — `DESIGN_SPEC` 第 3 節指定軸色 `#CFD5DD`，但實測它在白底只有
   **1.48:1**，與同一份規範第 2 節「非文字元素 ≥ 3:1」直接衝突。取對比度門檻，軸色加深。
5. **圖表序列橘改用 `#C2660C`** — 原 `#E8871E` 白底實測 **2.65:1** 未達 3:1。
   規範第 2 節本來就允許「加深或加深色描邊」，此處選加深。
6. **重置不還原分頁位置** — `DEV_SPEC` 第 1 節要求重置把 state 完整還原；本專案保留當前分頁，
   因為老師在實驗模式按重置卻被踢回模擬模式是干擾，不是還原。資料、開關、滑桿全部還原。
7. **觸控命中區用 `width:max(100%,44px)`** 而非模板的 `min-width:44px`。兩者等價，
   但 `DEV_SPEC` 檢核清單的驗收腳本寫死了測 `min-width`，對本專案會**假陰性**——
   驗收要改測 `getComputedStyle(el,'::after').width` 的計算值。

### 本次完成的修正

**A 級（教室現場會出事）**
- A1 補上整套 `--u` 等比放大（`uiScale()`／`clampScale()`／`?u=` 覆蓋參數）；CSS 字級全部 `calc(Npx * var(--u))`，
  canvas 內所有字級、線寬、點徑、器材幾何一律乘 `U`。**元件尺寸（44/34/38/22px）與固定容器內的圖示字級刻意不放大**。
- A2 座標軸改固定常數 `AXIS_V_MAX 60 / AXIS_M_MAX 160`，刻度節奏＝取樣節奏（每 10 mL 一格）；
  出界資料在被裁邊緣畫紅色方向三角＋「有 N 個點超出座標範圍」提示，資料本身完整保留。
- A3 補上 `:active` 按下回饋（`.app` 關掉了 `-webkit-tap-highlight-color`，這是成對規則）。
- A4 原本圖表格線迴圈次數與輸入值成正比（實驗模式多打幾個 0 就會凍結瀏覽器），
  **固定軸之後迴圈次數變成常數，此風險自然消除**。

**B 級（規範明文）**
- B5 補上 `watchDPR()`（換螢幕／接投影機時 DPR 變、CSS 尺寸不變，canvas 會糊掉）。
- B6 承載資訊的文字由 `--c-text-3`（3.77:1）改為 `--c-text-2`（8.17:1）：說明文字、空狀態、
  滑桿 min/max、「清空」鈕、表頭、資料組計數、canvas 刻度數字。
- B7 序列橘加深（見偏離 5）。
- B8 每組資料加代號 A–E，圖上標在擬合線右端、右欄標在色點內，顏色不再是唯一區辨方式。
- B9 「已達 5 組上限」「量筒已滿」「表格已滿」改為文字狀態列說明；去皮開關因放不下對照組而回彈時，
  一併以 `flashStatus()` 說明原因（原本是靜默回彈）。
- B10 `M` 一詞兩義修正：量筒質量一律寫 `M`<sub>量筒</sub>（下拉選單內不能放標籤，改寫成「質量 45 g」）。

**C 級**：C11 密度五檔、C12/C13 依裁示維持現狀並登記偏離、C14 新增「全部重置」按鈕
（並把 `fa-rotate-left` 讓給重置，倒空量筒改用 `fa-droplet-slash`）。

顯示位數集中到 `DEC = { rho:2, mass:1, vol:0 }`，各處不再硬編 `toFixed(n)`
（`runKey()` 內的 `toFixed(2)` 是 key 正規化，刻意不走 `DEC`）。

### 驗證狀態

**已在 Browser pane 驗過（機器斷言）**
- 物理模型手算對照：水 D=1.00、量筒甲 45 g、未去皮 → 10/20/30 mL 的讀數為 55/65/75 g，
  擬合斜率 1.0000（＝密度）、截距 45.0000（＝量筒質量），完全吻合。
- 固定軸的關鍵效果：加入第二組液體（食鹽水 D=1.20）後，**第一組資料點的螢幕座標完全未變**。
- 出界：3 個超界點資料完整保留、圖上不繪、紅色標記確實畫出（畫布紅色像素 1646）。
- 重置：資料組歸零、水位歸零、開關與 DOM 一致。
- `U` 與 CSS `--u` 一致；`?u=2.3` 被 `clampScale` 正確鉗到 1.742；按鈕維持 44px、圖示鈕維持 34px。
- 觸控命中區：`.icon-btn`/`.link-btn`/`.navtab`/`.ds-btn` 命中盒皆 ≥ 44×44。
- iPad 直向 810×1080：單欄、左右欄摺入底部分頁、面板可開可收。

**待真機驗證（Browser pane 驗不到，不得當成已通過）**
- [ ] 外觀、比例、資訊位置、手感——機器斷言完全沒驗到這四類。
- [ ] 教室大屏實機（本次只用 `?u=` 模擬，沒有真的 3000+ 實體像素裝置）；站 6 公尺外可讀性。
- [ ] iPad Safari 觸控拖曳、`:active` 回饋手感。
- [ ] 換螢幕／接投影機時 `watchDPR()` 是否真的補畫。
- [ ] 依視窗尺寸重算 `U` 的路徑：Browser pane **不派送 `resize` 事件**（見 `pitfalls-services.md` 第 3 條），
      本次只驗到「重新載入後算得對」，真正的即時 resize 未驗。
- [ ] `file://` 雙擊直開（本次是透過 localhost 驗的）。

---

## 2026-08-23　教學現場視角的全面審查與修正

使用者要求就四個面向審查：① 功能是否有誤 ② 對國中生是否過於複雜 ③ 平板／觸控大螢幕的字級與顏色
④ 有無多餘說明文字。以三個 opus agent 平行審查（功能邏輯／認知負荷／觸控視覺），
主對話另在 Browser pane 實測互動行為，agent 提報的行號逐一親自核對後才動手。

### 使用者裁示（1 題複選）

「移除既有元素」與「修正缺陷」分開處理，只有前者需要裁決：

| 項目 | 裁示 |
|---|---|
| 純裝飾元素（天秤綠色電源點、底部重複標題 `nav-note`、零功能的灰色假把手 `.handle`） | **刪** |
| 「顯示格線」開關 | **從學生畫面移除**；`state.showGrid` 維持預設 true，格線照常顯示（不牴觸 2026-08-22 裁示 3） |
| 說明彈窗裡的探究結論（「換量筒→平行、密度愈大線愈陡」） | **刪**——那是要學生自己發現的答案 |
| 右欄「清空」鈕 | **保留**。改以文案區分三個清除鈕：「清掉全部的線」／「清空表格」／「全部重置」 |

### 本次修正（依嚴重度）

**功能（A 級）**
- **F1 去皮切換會抹掉對照組資料點**（`exist.points = conv` 是整組覆寫）。改為以體積為鍵合併：
  同體積用換算值取代，對照組自己量過的其他點保留。原本量好 3 點的組，切一次去皮再切回來只剩 1 點、擬合線消失。
- **F2 連點「加 10 mL」會吃掉中間的資料點**。`update()` 原本只在抵達 `targetV` 的那一幀落一個點，
  學生連按時 `targetV` 一次跳到 50，圖上只剩一個孤點。改為回傳「這一幀跨過的所有取樣刻度」，逐一 `commit`。
  刻意不用「注水中禁用按鈕」解，連按本來就是合理操作。
- **F3 換液體／量筒／滑桿時量筒被靜默倒空**。`emptyCyl()` 加上 `reason` 參數，
  量筒內真的有液體時以 `flashStatus()` 說明「已倒空重來、圖上的線會留著」。
- **F4 `curKey` 失效時 `targetV` 沒歸零**（去皮 else 分支、刪除當前組、清空全部三處）。
  新增 `dropCurrentRun()` 統一處理，否則下次注水從半滿續加，圖上冒出孤點。
- **F5 量筒畫著 60 mL 刻度卻只能加到 50**。刻度迴圈改跑到 `MAX_V`；`CAP` 仍是 60，維持筒口留白。
- **F6 實驗模式重複按「加入圖表」會建出完全相同的線**。加入前比對 `points` 去重，並說明是哪一組。
- **F7 質量為負的點畫不出出界標記**。`drawOutMark()` 補 `underM` 分支（原本三角畫在 clip 區外被裁掉，
  變成「提示說有 N 個超界點、圖上一個都找不到」）。簽名改為帶入 `H` 與 `padB`。
- **F8 提示卡把 12.5 mL 顯示成 13**，與學生自己填的表格打架。非整數改顯示 1 位小數。

**視覺與觸控（B 級）**
- **V1 同一台 iPad 轉個方向就掉一級字**。兩處都要改：`uiScale()` 改用「螢幕長邊」判定裝置級別
  （寬度會隨旋轉改變，820×2=1640 掉到平板門檻 2000 以下）；`clampScale()` 的 `cssW/900` 是
  三欄並排的保護，&lt;900px 已摺成單欄，照樣套用會把 iPad 直向壓回 1。
- **V2 圖上代號必然重疊**。`drawSeriesLabel()` 原本完全沒有碰撞檢查，而相鄰兩種內建液體在 60 mL
  只差 6 g，比圓的直徑還小——「換液體看斜率怎麼變」這個主要活動必定讓代號疊死。加入上下推擠。
- **V3 天秤的 9px「TARE」** 是全頁最小的字，卻承載「有沒有歸零」。改為 12px 的「已歸零」。
- **V4 代號圓改為白底＋彩色粗環＋深色字**（圖上與右欄 `.ds-dot` 同一套）。原本彩底白字在
  綠/橘/紫/青上只有 3.4–4.3:1，而代號正是顏色被投影洗掉後唯一的區辨依據，它必須先自己讀得清楚。
  另新增 `pointPath()`：五組資料的**資料點形狀**也不同，作為不受投影影響的第二條區辨通道。
- **V5**「新增一列」40px → 44px。**V6** 右上兩顆圖示鈕 gap 8→12px（44px 命中盒原本互相重疊）、
  `.ds-btn` 相鄰間距 +6px（眼睛與刪除原本零死區貼合）、`.ds-list` 補 `padding-right` 讓刪除鈕的
  命中盒不被 `overflow` 裁掉。
- **V7 隱藏態**由整列 `opacity:.42` 改為只淡化色點與名稱（`.65`）且名稱改用 `--c-text`：
  名稱 2.04→**4.92:1**、按鈕 1.63→**3.77:1**。
- 提示卡的資料組名稱原本用序列色當文字（3.4–4.3:1），顏色改由前置小圓點承擔，文字回到正常字色。

**文字**
- 刪「最小平方法」（高中以上術語）、刪 `expHint` 整句（同一件事畫面上已講三遍）、
  右欄空狀態改「還沒有畫任何線。」（與圖表中央那句原本是同一句話的兩個版本，措辭還不一致）。
- 去皮副標不再提「截距」：改為「現在的讀數包含量筒本身的重量」／「已經扣掉量筒，只秤液體的重量」。
  **HTML 初始值與 `syncUI()` 兩處都要改**——只改 HTML 會被 `syncUI()` 立刻覆寫回去。
- 資料組名稱 `水・未含量筒` → `水（已扣掉量筒）`（中黑點加雙重否定讀起來像資料庫欄位）。
- 計數器 `0/5` → 平時不顯示，快滿才說「還可以放 N 條」「已經放滿 5 條」。
- 說明彈窗「截距就是量筒質量 M」補上下標 `M<sub>量筒</sub>`（2026-08-22 B10「M 一詞兩義」的殘留）。

### 新增的規範偏離登記

承 2026-08-22 的 7 條，本次新增 4 條，**都是刻意的**：

8. **`--c-bd-ctl` 由 `#CFD5DD` 加深為 `#8A93A1`**（1.48→3.10:1）。同 2026-08-22 偏離 4、5 的理由：
   `DESIGN_SPEC` 第 3 節指定的控制項邊框色與第 2 節「非文字元素 ≥3:1」直接衝突，取對比度門檻。
   連帶讓開關的「關」態看得出是個開關（白鈕 vs 軌道 3.10:1）。
9. **`--c-warn` 由 `#D64545` 加深為 `#C62F2F`**（4.38→5.46:1）。canvas 的 `C.out` 同步。
10. **資料組代號改白底彩環深字，不是彩底白字**。序列色維持不變（它們作為「環」只需 3:1，
    全部達標）；改的是文字與底色的分工。這是在「不動已定案的序列色」前提下讓代號達標的唯一解——
    若要讓白字達 4.5:1 就得把五色全部加深，明度全距反而更小、區辨更差。
11. **`clampScale()` 的寬度保護加上 `cssW >= 900` 條件**。原式對所有寬度套用 `cssW/900`，
    但 &lt;900px 已由 media query 摺成單欄，該保護不再適用。

### 驗證狀態

**環境限制（先講）**：本次 Browser pane 無法顯示，`visibilityState` 恆為 `hidden`、
**rAF 每秒 0 次**，凡依賴注水動畫的路徑一律無法端到端實跑。此外該環境的 `screen.width/height` 回報 0。

**已實測（機器斷言）**
- 初始化完整跑完（7 個液體選項、10 個表格輸入格、無執行期錯誤）——四項元素刪除後無殘留引用。
- 換液體會出現「量筒已倒空重來」提示；資料組名稱、計數文字、去皮副標兩態文字全部正確。
- 對比度實算：警示紅 5.46、控制項邊框 3.10、開關軌道 3.10、代號圓文字 15.56、
  隱藏態名稱 4.92／按鈕 3.77，全部達標。
- 觸控：說明鈕命中 44×44、分頁 90×44、「清掉全部的線」84×44；兩顆圖示鈕間距 12px 不再重疊。
- 實驗模式重複「加入圖表」被擋下並說明是哪一組；負質量的紅色出界三角確實畫出（356 個紅色像素）。
- `clampScale()`：810×1080 加 `?u=1.15` 得到 `--u=1.15`（修正前會被壓回 1），
  最小字級 11.5px→13.22px。
- **F1／F2 的演算法以 node 單獨驗證**（`scratchpad/verify-logic.js`，8 項全過）：
  正常節奏、連按、單幀跨完全程、倒空後重加、浮點邊界、三種合併情境。

**待真機驗證（此環境驗不到，不得當成已通過）**
- [ ] `uiScale()` 的裝置分級：本環境 `screen.width` 回 0，走不到平板分支。
      **真機 iPad 直向是本次修正的主要目標，必須實測**（預期直向與橫向都拿到 1.15）。
- [ ] F1／F2 在真實動畫下的端到端行為：連按「加 10 mL」是否真的落 5 個點、
      去皮切換來回是否真的保住對照組的點。**演算法已驗，但整條路徑沒跑過。**
- [ ] 外觀、比例、手感——機器斷言完全沒驗到。代號圓改成白底彩環後的視覺是否仍好認，需目視。
- [ ] 資料點五種形狀在投影機上的實際區辨效果。
- [ ] 教室大屏實機；站 6 公尺外可讀性。
- [ ] `file://` 雙擊直開。

---

## 2026-08-24　課堂用語、方程式顯示、實驗數據可修改

使用者提出四項需求，動手前以 `AskUserQuestion` 問了三個會改變作法的決策點。

### 使用者裁示（3 題）

| # | 問題 | 裁示 |
|---|---|---|
| 1 | 方程式顯示位置 | **畫在圖上每條線旁邊**（不是右欄清單，也不是兩處都放） |
| 2 | 實驗模式怎麼讓學生改數字 | **表格記住自己畫的是哪一條線**，改完按鈕變「更新圖上的線」；另加每列刪除鈕 |
| 3 | 兩個勾選的連動 | **方程式從屬於直線**：直線關掉時方程式一起關並鎖住 |

### 推翻先前裁示（重要）

**2026-08-22 裁示 3「顯示直線與截距預設開」本次改為預設關**，理由由使用者提出：
學生要先看見散點、自己判斷「這些點排成一直線」，線一開始就畫好等於先給答案。
連帶「顯示方程式」也預設關。**規範偏離登記第 1 條（輔助疊層預設開）隨之作廢**——
本專案現在回到 `DESIGN_SPEC` 第 4 節「教學輔助疊層一律預設關」的原始規定。
偏離 2（擬合線外插畫滿全寬）不受影響，仍然維持。

### 本次修改

**用語**
- 全檔移除「去皮」（介面文字、說明彈窗、狀態訊息、程式碼註解共 8 處），改用課堂慣用的
  **「天秤歸零」**。實驗模式的「質量含量筒（沒有去皮）」→「質量含量筒（天秤沒有歸零）」。

**方程式（新功能）**
- 新增 `state.showEq` 與從屬勾選項「顯示方程式」，畫在圖上、緊接在代號圓右邊，
  整塊右緣對齊線的右端，底下墊 88% 白底（它必然壓到格線與別條線）。
- 位數由 `DEC` 決定，不另外硬編：**斜率 2 位**（＝密度，與液體選單「D = 1.00」同位數）、
  **截距 1 位**（＝量筒質量，與天秤讀數同位數）。截距為 0 時整項不寫（`M = 0.80 × V`），
  負截距用 `−`。位數對得上，學生才能把線上的數字與畫面其他地方的數字連起來。
- **修掉一個既有的防碰撞缺陷**：原本推擠量以「被撞到的那個框」為基準，
  `|b.cy - cy|` 剛好等於 `gap` 時浮點誤差會讓它反覆推回同一格，guard 用完就靜靜放棄。
  實測五組全開時有三個標籤疊死在同一高度。改為從自己的理想位置向上下交替搜尋空位。
- **代號與方程式改在最後統一畫，且照「線的高低」排序**，不照資料組順序。
  照資料組順序的話，圖上最高的線會排在後面被推到別人下方，方程式離自己的線愈來愈遠。

**實驗模式可修改**
- `state.expLinkId` 記住這張表格畫出的是哪一條線。有連結時「加入圖表」→「更新圖上的線」，
  改數字後直接改那一條，不再多長出一條錯的。名稱與「含量筒」勾選也一起更新。
- 「清空表格」→「清空表格，換下一組」，同時清掉名稱與連結。
- 每列加 `✕` 刪除鈕（34×44，命中盒 44×44）。剩兩列時**不停用按鈕**，改在按下時說
  「至少要留兩列——兩個點才畫得出一條直線」（比照 B9：邊界情境用文字說明，不用灰色傳遞資訊）。
- 右欄刪掉連結中的那條線 → 連結解除，按鈕退回「加入圖表」。
- `expNote` 說明文字隨階段換句，且**HTML 初始值與 `syncUI()` 的「尚未連結」分支一字不差**
  （承 2026-08-23 的教訓：只改 HTML 會被 `syncUI()` 立刻覆寫）。

### 規範偏離登記的變更

- **第 1 條作廢**（輔助疊層預設開）——見上方「推翻先前裁示」。
- **新增第 12 條**：從屬勾選項的鎖定態**只把方框變灰，文字維持 `--c-text-2`（8.17:1）**。
  一般 disabled 慣例會把整個標籤淡化，但「要先打開上面的『顯示直線』」正是學生此刻
  最需要讀到的一句，用對比度懲罰唯一的解釋是本末倒置（同 `DESIGN_SPEC` 第 2 節精神）。

### 驗證狀態

**環境限制（先講）**：Browser pane 仍無法顯示，`visibilityState` 恆為 `hidden`、rAF 不跑、
**截圖逾時**。凡依賴注水動畫的路徑一律無法端到端實跑；本次一律以直接呼叫 `drawChart()`
＋ `getImageData()` 像素斷言取代目視。

**已實測（機器斷言）**
- 全檔 0 處「去皮」；`chkTare` 標籤讀出「天秤歸零 / 現在的讀數包含量筒本身的重量」。
- 方程式字串位數：`M = 1.00 × V + 45.0`、`M = 0.80 × V`、`M = 0.99 × V + 44.9`、`M = 1.00 × V − 3.5`。
- 開關連動四態全對；關掉直線時 `showEq` 與 DOM `checked` 同步歸零；重置後兩者皆回到關且鎖住。
- 像素斷言：方程式開啟時深色像素 40→367（文字確實畫出）；關掉直線時序列色像素 1012→241
  （只剩資料點，線確實消失）。
- 防碰撞 0 重疊：五組不同斜率、五組平行線（代號 x 完全一致）、兩組、五組只有代號，四種情境全過。
- 實驗模式端到端：打錯 651 → 改 65.1 → 資料組維持 1 條、擬合 a=1.005 b=45.1；
  刪列後更新、改名後更新、清空後可新增第二條 B、右欄刪掉連結線後按鈕退回「加入圖表」。
- 對比度：鎖定態標籤與提示 8.17:1、刪除鈕 8.17:1。命中盒：刪除鈕 44×44、兩個勾選標籤 44px 高。
- 810×1080 直向：抽屜按鈕未換行未擠壓（44px 高），輸入格 346px、刪除鈕 34px。
- 無執行期錯誤（console 0 筆）。

**待真機驗證（此環境驗不到，不得當成已通過）**
- [ ] 方程式的**實際可讀性與版面觀感**——像素斷言只證明「畫出來了」，沒有證明「好看、讀得到」。
      特別是五組全開時四行方程式堆在右上角的擁擠程度，必須目視。
- [ ] 方程式白底墊層壓住線條的程度是否可接受（實測會蓋掉約 23% 的線段像素）。
- [ ] 教室大屏與投影機上的方程式字級（11.5px × U）是否夠遠距離讀。
- [ ] iPad Safari：`✕` 刪除鈕的觸控手感、`type=number` 鍵盤彈出時表格是否被鍵盤蓋住。
- [ ] `file://` 雙擊直開。
- [ ] 承 2026-08-23 未消化的項目：`uiScale()` 裝置分級、F1／F2 在真實動畫下的端到端行為。

---

## 尚未處理（本次刻意沒做）

1. ~~**沒有 git repo**~~ — **已解決**（2026-08-23 查核）。本資料夾已是獨立 repo，
   目前有 `ebeb26a`（初始版本）與 `853934c`（規範對齊）兩個提交。2026-08-23 的修正尚未提交。
2. **設計稿散在根目錄**（`Canvas.dc.html`、`DensityVolumeLab.dc.html`、`support.js`，共 169 KB）。
   motion-graphs-sim 是收在 `design_handoff/`，本專案未比照整理。
3. **Font Awesome 走 CDN**。規範允許，但離線教室會讓所有圖示變空白方框。是否本地化子集未決。
4. **本次發現、但尚未回寫 `_shared/` 的問題**（回寫會動到共用規範，需另行決定）：
   - 模板把 `.logo`／`.icon-btn` 的字級也乘上 `--u`，但它們的容器是固定 28/34px，
     放大倍率一高圖示就會撐破容器。本專案刻意不放大這類「固定容器內的圖示字級」。
   - `DESIGN_SPEC` 第 3 節的軸色與第 2 節的對比度門檻互相衝突（見偏離 4）。
   - `DEV_SPEC` 檢核清單的命中區驗收方法綁死 `min-width` 寫法（見偏離 7）。
   - 只掛 `window.addEventListener('load')` 補算 `--u` 是無效的——script 在 body 末尾執行時
     `load` 多半已經觸發過。本專案改為 rAF ＋ `setTimeout(0)` ＋ `setTimeout(400)` 三重補算。

---

## 2026-08-24　教學現場問題盤點（已查證，尚未修，等使用者裁決）

使用者要求檢查「國中學生操作會不會遇上問題或認知負荷過高」。以下每一條都在
Browser pane 實際重現過，不是推測。**都還沒有動手修，等裁決。**

### A 級——畫面對學生說謊或做不下去（**兩條都已於本日修好**）

- **A1 漏填一格會被靜默丟掉。✅ 已修。** 實驗模式體積欄預填 10/20/30/40/50，學生若只漏填第 3 列的質量，
  按「加入圖表」後圖上只有 4 個點，**狀態列一個字都沒有**。實測：填 4 列 → 圖上 4 點
  （體積 10/20/40/50），`status` 為空字串且 hidden。這違反本專案自己的 Fail loud 原則
  （超出座標範圍都會標紅色三角並說「有 N 個點超出」，漏填卻完全不說）。

  **修法**：加入／更新後一律以 `expReport()` 據實回報「圖上有 N 個點」，不是只在出事時才說；
  被略過的列另外點名並把該列輸入框標紅（`.inp.miss`，`--c-warn` 邊框 5.46:1）。
  **紅框只是第二條通道**——表格沒有列號，光靠顏色學生數不出來自己漏的是第幾列，所以文字要點名。

  **判定哪幾列算「漏填」是這條的難處**：體積欄預填了 10～50，所以「有體積、沒質量」正是
  尚未動過的預設狀態，若一律警告，**每次只量兩點的正常操作都會被標三列紅框**（第一版實作
  就是這樣，實測後收緊）。最終規則：① 中間空一格（`i < lastOk`）幾乎一定是漏填 → 警告；
  ② 末尾留白是「只量到這裡」→ 不警告，但「圖上有 2 個點」這句照說；
  ③ 質量填了卻畫不上去（體積空白或負數）一律警告，那是明確的輸入被丟掉。

- **A2 進入「實驗模式」看不到任何可以填的東西。✅ 已修。** `<900px`（iPad 直向 810 就是）左欄摺成浮出面板，
  實測 `panelExp` 寬度為 0，學生必須先發現底部那顆叫**「設定」**（齒輪圖示）的 chip 才找得到表格。
  模擬模式沒這問題（主要動作「加 10 mL」在底部抽屜），實驗模式則是**進去之後無事可做**。

  **修法**：兩條一起做。① `<900px` 切到實驗模式時自動展開該面板（`show-settings`）；
  ② chip 的文字與圖示隨模式改寫——模擬模式「設定 / fa-gear」、實驗模式「數據輸入 / fa-table-list」。
  ≥900px 不受影響（實測 1280×800 下 `body` 無 class、面板仍是 307px 的正常欄位、chip 隱藏）。

**A1／A2 的驗證（機器斷言，同樣沒有目視）**
- A1 四情境：漏中間第 3 列 → 4 點＋點名第 3 列＋紅框；只量兩點 → 2 點、**不警告、無紅框**；
  質量填了但體積清空 → 點名＋紅框（末尾也算）；全部填好 → 5 點、完全不提略過。
  補填後紅框即時消失（不重建表格，避免打字中焦點被抽掉）。
- A2：810×1080 切進實驗模式 → 面板寬 782、10 個輸入格、chip 變「數據輸入」＋`fa-table-list`；
  切回模擬模式 → 面板收起、chip 回「設定」、注水鈕可用。1280×800 完全不受影響。

### B 級——會誤導或製造多餘負荷

- **B3「質量含量筒（天秤沒有歸零）」這個勾選其實什麼都沒做。** 查證：`state.expIncl`
  在全檔只用於組出資料組**名稱**的後綴，不參與任何計算、不改動資料點。它看起來像個會
  「幫我把量筒扣掉」的開關，實際只是貼標籤。學生勾錯不會有任何後果，也學不到東西。
  建議：改成純標示文字（例如副標「只是幫這組資料取名，不會改動你填的數字」），
  或乾脆讓它真的做事（勾掉時把所有質量減去一個學生輸入的量筒質量）。後者要多一個輸入欄。

- **B4 自訂密度調到 1.00 會和「水」畫出兩條完全重疊的線。** `runKey()` 用 `liquidId` 當第一段，
  所以 `water|1.00|y|45` 與 `custom|1.00|y|45` 是兩條不同的線，但資料點完全重合。
  滑桿的五個檔位 0.6/0.8/1.0/1.2/1.4 裡有三個與內建液體同值（酒精 0.80、水 1.00、食鹽水 1.20），
  很容易撞上。圖上會出現兩個代號、兩條疊死的線，還白吃兩個名額。
  建議：`runKey()` 改用密度值而非 `liquidId`（同密度同量筒視為同一條），或滑桿檔位避開內建值。

- **B5「未知液體A」的密度 0.60 g/cm³ 對不到任何常見液體。** 常見的低密度液體最低約 0.7
  （汽油、乙醚）。學生從斜率反推出 0.60 之後問「所以這是什麼」，老師沒有答案。
  建議改為 0.70 或 0.79；未知液體B 的 1.10 尚可（糖水、甘油水溶液）。**這是教學內容決定，
  不是程式問題，留給老師。**

- **B6「模擬模式／實驗模式」的命名。** 「實驗模式」實際做的事是「把我在實驗室量到的數字打進來」，
  但字面看起來比「模擬模式」更像會有互動實驗。國中生第一次點進去容易搞錯。
  建議：「模擬」／「輸入我的數據」。**牽動底部導覽列與多處文案，屬文案決定，留給老師。**

### C 級——已知但影響小

- **C7 直線外插到 60 mL，量筒卻只能加到 50。** 這是 2026-08-22 裁示 4 的刻意設計
  （「照這條線，60 mL 會是幾克？」的預測活動），但畫面上沒有任何一句話點出
  「超過 50 的那一段是預測，不是量到的」。老師知道，學生不一定。
  建議：在說明彈窗補一句，不要加在主畫面（主畫面文字已經夠多）。

- **C8 三顆清除類按鈕同時在場。** 實驗模式下「清掉全部的線」（右欄）、「清空表格，換下一組」、
  「全部重置」三顆並存。2026-08-23 已用文案區分過，本次「清空表格」再加了「換下一組」，
  語意更清楚了，但三顆仍然是三顆。暫不再動。

---

## 2026-08-26　全螢幕鈕：跨模擬缺陷回流修正

本次改動不是從這支模擬發現的，是 `motion-graphs-sim` 修完後回頭比對，發現這裡是**同一段舊寫法**。

**問題**（使用者在教室觸控大屏實測，筆電與 iPad 完全正常）：按網頁的全螢幕鈕確實進得去，
再按一次卻退不出來。那台瀏覽器的 `document.exitFullscreen()` 會回報成功、派送
`fullscreenchange`、把 `fullscreenElement` 設成 `null`，**畫面卻沒有離開全螢幕**，
之後點頁面空白處狀態又自己跳回全螢幕。

**修正**（`index.html` 全螢幕區塊，照 `_shared/DEV_SPEC.md` 5.2 新寫法）：

- 判斷改為三個旗標一起看（`fullscreenElement` / `webkitFullscreenElement` /
  `webkitIsFullScreen`）——iPadOS Safari 對非影片元素有時只設最後一個。
- 退出改走 `exitFull()`：依序試 `exitFullscreen` / `webkitExitFullscreen` /
  `mozCancelFullScreen` / `msExitFullscreen`，呼叫標準版後 150ms **用視窗高度**複檢，
  仍佔滿螢幕就補一次 webkit 版。**複檢不能問 `fullscreenElement`**，它在那台機器上已經是 `null`。
- 圖示與 `aria-label` 跟著狀態切換（`fa-expand` ↔ `fa-compress`），並監聽
  `fullscreenchange` ／ `webkitfullscreenchange`。這一條的理由是**可診斷性**：
  狀態不可見時，這個 bug 完全無從判斷根因。

**驗證**：攔截四個 API 記錄呼叫序列——未全螢幕時按下走 `request`、全螢幕時走 `exit`；
正常情境只呼叫 `standard`，偽造「狀態說退出但畫面仍滿版」得到 `standard → webkit`；
圖示雙向切換正確。另跑冒煙測試確認加水、重置、兩個分頁未受影響，console 無錯誤。
**大屏由使用者實測通過**（2026-08-26）。

技術細節見 handbook `pitfalls-frontend.md` 第 16 條；三種驗收裝置的分工見專案 memory
`sim-test-devices`。**檔案現況**：`index.html` 1531 行。

---

## 2026-08-30　⚠ 待改：三欄比例與圖表位置（全域規範已改，本模擬尚未跟上）

**狀態：已排期，尚未動工**（使用者 2026-08-30 裁示「排期修改」，非追認為例外）。

### 規範怎麼變的

`_shared/design/DESIGN_SPEC.md` §1 於 2026-08-30 全域修訂兩條，來源是 `dissolution-sim` 的接稿裁示：

| 項目 | 修訂前 | 修訂後 |
|---|---|---|
| 三欄比例 | 固定 `1fr 2fr 1fr` | 設定 : (模擬＋數據) ＝ **1 : 3** 固定；模擬 : 數據由各模擬特性決定，取 `1:1`（`1fr 1.5fr 1.5fr`）或 `2:1`（`1fr 2fr 1fr`） |
| 圖表位置 | 右欄「只放彙總與靜態資訊」，圖表留中欄 | **圖表一律放右欄**，與中欄畫布水平對齊；iPad 直向（<900px）例外，圖表改回畫布下方不摺進抽屜 |

使用者原話：「我希望以後所有的圖表數據等全部都可以在右側欄」。

### 本模擬哪裡不合

- `index.html:83` — `grid-template-columns:1fr 2fr 1fr`。
  **這一條其實仍合規**（屬新規範的 `2:1` 情形），除非改成圖表在右欄，那就要換成 `1fr 1.5fr 1.5fr`。
- `index.html:87` 起 — 註解寫「中欄上下二分：器材 44% / 座標圖 56%；實驗模式時器材收起，圖表佔滿」。
  **這是不合的那一條**：座標圖在中欄與器材上下堆疊，新規範要求移到右欄。

### 要改什麼（動工時的清單）

1. 座標圖從中欄移到右欄，中欄只留器材；三欄比例改 `1fr 1.5fr 1.5fr`
2. 右欄原有的數據卡與圖表共存，參考 `_shared/design/layout.html` 的右欄結構
   （**注意該檔的 `min-height:0`**：少了它，flex 會依內容分配高度，右欄會撐出 main 並被底部抽屜蓋住——
   2026-08-30 更新 layout.html 時實際踩到，已在該檔加註解）
3. 「實驗模式時器材收起、圖表佔滿」的既有行為要重新設計——圖表移到右欄後，中欄收起器材會留下空欄
4. iPad 直向（<900px）：圖表改回畫布下方，不摺進抽屜
5. 改完重跑 `DEV_SPEC` 第 8 節檢核清單，並在三種驗收裝置實測（見專案 memory `sim-test-devices`）

### 為什麼不是現在改

本模擬**已上線**（GitHub Pages），且 2026-08-26 才剛做完全螢幕鈕修正並經使用者大屏實測通過。
規範修訂當下的主線工作是 `dissolution-sim`，本項不阻塞它，故排期而非立即動工。
