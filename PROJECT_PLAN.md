# Density_experiment — 專案計畫與裁示紀錄

**模擬主題**：液體的體積與質量關係（國中理化「密度」單元）
**核心教學命題**：M = D × V + M<sub>量筒</sub>。**斜率＝液體密度、截距＝量筒質量**；
同液體換量筒 → 兩線平行；換密度 → 斜率改變。
**檔案**：`index.html`（單檔交付）。`*.dc.html` 與 `support.js` 是 2026-07-27 的 Claude Design 稿，非執行檔。

---

## 2026-08-22　依運動學模擬器經驗做規範對齊

`_shared/` 的 `DEV_SPEC.md` / `DESIGN_SPEC.md` / `template/index.html` 在 2026-08-20～22 依
motion-graphs-sim 的實作經驗大幅增修（新增 5.4 等比放大、第 7 節圖表行為、第 2 節對比度門檻、
第 9 節教學內容規範等）。本模擬完成於 2026-08-18，停在舊版規範，本次補齊。

### 使用者裁示（4 題，AskUserQuestion）

| # | 問題 | 裁示 | 取代了什麼 |
|---|---|---|---|
| 1 | 座標軸範圍 | **全部固定**，超出裁切＋出界標記 | 取代原本「依資料自動縮放」（`Math.ceil(maxV…)`／`nice()`）。模擬與實驗兩模式共用同一組固定軸 `V 0–60 / M 0–160` |
| 2 | 自訂密度滑桿 | **五檔 0.6 / 0.8 / 1.0 / 1.2 / 1.4** | 取代原本 `step="0.01"`（81 檔連續）。預設值一併由 1.05 改為 1.00（原值不在檔位上） |
| 3 | 輔助疊層預設狀態 | **「顯示直線與截距」「顯示格線」都維持預設開** | 明確**不採用** `DESIGN_SPEC` 第 4 節「教學輔助疊層一律預設關」。理由：這個模擬的核心命題就是那條直線，預設關會讓學生第一眼只看到散點 |
| 4 | 擬合直線範圍 | **維持畫滿全寬**（外插到尚未量測的體積） | 明確**不採用** `DESIGN_SPEC` 第 7 節「不劇透」。理由：「照這條線，40 mL 應該是幾克？」的預測活動是本模擬的教學重點 |

### 規範偏離登記（`DEV_SPEC` 10.4 要求）

以下每一條都與 `_shared/` 的現行規範不同，**是刻意的，不要改回去**：

1. **輔助疊層預設開**（裁示 3）— 偏離 `DESIGN_SPEC` 第 4 節。
2. **擬合線外插畫滿全寬**（裁示 4）— 偏離 `DESIGN_SPEC` 第 7 節「不劇透」。
3. **密度滑桿檔位是小數**（0.6/0.8/…）— `DESIGN_SPEC` 第 4 節寫「禁止小數檔位」，
   但那條是為運動學的加速度寫的；密度本質就是小數，改成整數無意義。檔數（5）仍遵守 3–7 檔。
4. **座標軸線改用 `#7A8496`** — `DESIGN_SPEC` 第 3 節指定軸色 `#CFD5DD`，但實測它在白底只有
   **1.48:1**，與同一份規範第 2 節「非文字元素 ≥ 3:1」直接衝突。取對比度門檻，軸色加深。
5. **圖表序列橘改用 `#C2660C`** — 原 `#E8871E` 白底實測 **2.65:1** 未達 3:1。
   規範第 2 節本來就允許「加深或加深色描邊」，此處選加深。
6. **重置不還原分頁位置** — `DEV_SPEC` 第 1 節要求重置把 state 完整還原；本專案保留當前分頁，
   因為老師在實驗模式按重置卻被踢回模擬模式是干擾，不是還原。資料、開關、滑桿全部還原。
7. **觸控命中區用 `width:max(100%,44px)`** 而非模板的 `min-width:44px`。兩者等價，
   但 `DEV_SPEC` 檢核清單的驗收腳本寫死了測 `min-width`，對本專案會**假陰性**——
   驗收要改測 `getComputedStyle(el,'::after').width` 的計算值。

### 本次完成的修正

**A 級（教室現場會出事）**
- A1 補上整套 `--u` 等比放大（`uiScale()`／`clampScale()`／`?u=` 覆蓋參數）；CSS 字級全部 `calc(Npx * var(--u))`，
  canvas 內所有字級、線寬、點徑、器材幾何一律乘 `U`。**元件尺寸（44/34/38/22px）與固定容器內的圖示字級刻意不放大**。
- A2 座標軸改固定常數 `AXIS_V_MAX 60 / AXIS_M_MAX 160`，刻度節奏＝取樣節奏（每 10 mL 一格）；
  出界資料在被裁邊緣畫紅色方向三角＋「有 N 個點超出座標範圍」提示，資料本身完整保留。
- A3 補上 `:active` 按下回饋（`.app` 關掉了 `-webkit-tap-highlight-color`，這是成對規則）。
- A4 原本圖表格線迴圈次數與輸入值成正比（實驗模式多打幾個 0 就會凍結瀏覽器），
  **固定軸之後迴圈次數變成常數，此風險自然消除**。

**B 級（規範明文）**
- B5 補上 `watchDPR()`（換螢幕／接投影機時 DPR 變、CSS 尺寸不變，canvas 會糊掉）。
- B6 承載資訊的文字由 `--c-text-3`（3.77:1）改為 `--c-text-2`（8.17:1）：說明文字、空狀態、
  滑桿 min/max、「清空」鈕、表頭、資料組計數、canvas 刻度數字。
- B7 序列橘加深（見偏離 5）。
- B8 每組資料加代號 A–E，圖上標在擬合線右端、右欄標在色點內，顏色不再是唯一區辨方式。
- B9 「已達 5 組上限」「量筒已滿」「表格已滿」改為文字狀態列說明；去皮開關因放不下對照組而回彈時，
  一併以 `flashStatus()` 說明原因（原本是靜默回彈）。
- B10 `M` 一詞兩義修正：量筒質量一律寫 `M`<sub>量筒</sub>（下拉選單內不能放標籤，改寫成「質量 45 g」）。

**C 級**：C11 密度五檔、C12/C13 依裁示維持現狀並登記偏離、C14 新增「全部重置」按鈕
（並把 `fa-rotate-left` 讓給重置，倒空量筒改用 `fa-droplet-slash`）。

顯示位數集中到 `DEC = { rho:2, mass:1, vol:0 }`，各處不再硬編 `toFixed(n)`
（`runKey()` 內的 `toFixed(2)` 是 key 正規化，刻意不走 `DEC`）。

### 驗證狀態

**已在 Browser pane 驗過（機器斷言）**
- 物理模型手算對照：水 D=1.00、量筒甲 45 g、未去皮 → 10/20/30 mL 的讀數為 55/65/75 g，
  擬合斜率 1.0000（＝密度）、截距 45.0000（＝量筒質量），完全吻合。
- 固定軸的關鍵效果：加入第二組液體（食鹽水 D=1.20）後，**第一組資料點的螢幕座標完全未變**。
- 出界：3 個超界點資料完整保留、圖上不繪、紅色標記確實畫出（畫布紅色像素 1646）。
- 重置：資料組歸零、水位歸零、開關與 DOM 一致。
- `U` 與 CSS `--u` 一致；`?u=2.3` 被 `clampScale` 正確鉗到 1.742；按鈕維持 44px、圖示鈕維持 34px。
- 觸控命中區：`.icon-btn`/`.link-btn`/`.navtab`/`.ds-btn` 命中盒皆 ≥ 44×44。
- iPad 直向 810×1080：單欄、左右欄摺入底部分頁、面板可開可收。

**待真機驗證（Browser pane 驗不到，不得當成已通過）**
- [ ] 外觀、比例、資訊位置、手感——機器斷言完全沒驗到這四類。
- [ ] 教室大屏實機（本次只用 `?u=` 模擬，沒有真的 3000+ 實體像素裝置）；站 6 公尺外可讀性。
- [ ] iPad Safari 觸控拖曳、`:active` 回饋手感。
- [ ] 換螢幕／接投影機時 `watchDPR()` 是否真的補畫。
- [ ] 依視窗尺寸重算 `U` 的路徑：Browser pane **不派送 `resize` 事件**（見 `pitfalls-services.md` 第 3 條），
      本次只驗到「重新載入後算得對」，真正的即時 resize 未驗。
- [ ] `file://` 雙擊直開（本次是透過 localhost 驗的）。

---

## 尚未處理（本次刻意沒做）

1. **沒有 git repo**。母倉 `.gitignore` 已排除本資料夾，但這裡沒有 `.git`，改壞了無法還原。
   本次修改前的原檔備份在 scratchpad（session 結束即失效），**建議儘快 `git init`**。
2. **設計稿散在根目錄**（`Canvas.dc.html`、`DensityVolumeLab.dc.html`、`support.js`，共 169 KB）。
   motion-graphs-sim 是收在 `design_handoff/`，本專案未比照整理。
3. **Font Awesome 走 CDN**。規範允許，但離線教室會讓所有圖示變空白方框。是否本地化子集未決。
4. **本次發現、但尚未回寫 `_shared/` 的問題**（回寫會動到共用規範，需另行決定）：
   - 模板把 `.logo`／`.icon-btn` 的字級也乘上 `--u`，但它們的容器是固定 28/34px，
     放大倍率一高圖示就會撐破容器。本專案刻意不放大這類「固定容器內的圖示字級」。
   - `DESIGN_SPEC` 第 3 節的軸色與第 2 節的對比度門檻互相衝突（見偏離 4）。
   - `DEV_SPEC` 檢核清單的命中區驗收方法綁死 `min-width` 寫法（見偏離 7）。
   - 只掛 `window.addEventListener('load')` 補算 `--u` 是無效的——script 在 body 末尾執行時
     `load` 多半已經觸發過。本專案改為 rAF ＋ `setTimeout(0)` ＋ `setTimeout(400)` 三重補算。
